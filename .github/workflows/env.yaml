on:    
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: Set API key
        run: echo "API_key =1234" >> $GITHUB_ENV

      - name: Use API Key
        run: |
          echo "::add-mask::$GITHUB_ENV"
          echo "Using API Key $GITHUB_ENV"
        
